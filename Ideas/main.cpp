//add input output stream header file
#include <iostream>

/*main part of the program
 * <</>> = left shift operator(insertion operator)/ right shift operator(extraction operator), :: = scope resolution operator, std:: = standard operation, cin/cout : character input(>>)/ output(<<)
 * data types: double(decimal), int(whole number), char(single character/ ''), bool(true/false), std::string =  " Sequence of texts ", \n/ endl,
 * string literal/ const(constant/read-only), overflow, truncated, namespace{x = 0}/using namespace, typedef/ using for aliases(typedef data type identifier/ using variable equals old data type)
 * arithmetic operator(+, -, *, /), ++ | +1 | +=1 / -- | -= (increment/ decrement), *=, /=, % (modulus/ remainder), precedence(parenthesis/multiplication & division/ addition & subtraction)
 * type conversion(implicit = automatic/ explicit = new data type), declaration/ assignment,
 */

int main(){
    std::cout << "I don't really like C++!";
    return 0;
}