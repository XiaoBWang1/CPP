//add input output stream  header file
#include <iostream>

/*main part of the program
 * << : left shift operator, :: = scope resolution, std = standard, cin/cout : character input/ output
 * data types: double(decimal), int(whole number), char(single character/ ''), bool(true/false), std::string =  "Sequence of texts ",
 * string literal/ con(constant/read-only),
 */
int main(){
    std::cout << "I love sushi!";

    return 0;
}